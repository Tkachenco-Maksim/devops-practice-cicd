name: Weekly Night Scan

on:
  schedule:
    # 22:30 в воскресенье (Вариант C3R)
    - cron: '30 22 * * 0'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run scan
        run: echo "Variant C3R scan running... System check OK."
