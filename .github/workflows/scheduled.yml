name: Weekly Night Scan

on:
  schedule:
    - cron: '30 22 * * 0'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run scan
        run: echo "Variant C3R scan running..."
